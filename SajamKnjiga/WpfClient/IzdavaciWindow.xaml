<Window x:Class="WpfClient.IzdavaciWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient"
        mc:Ignorable="d"
        Title="IzdavaciWindow" Height="450" Width="800">
    <Grid Margin="5">
        <DataGrid x:Name="dgIzdavaci"
                  AutoGenerateColumns="False"
                  RowBackground="White"
                  AlternatingRowBackground="#F0F0F0"
                  GridLinesVisibility="None"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  RowHeight="25">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#D3D3D3"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Sifra}" Width="60"/>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Naziv}" Width="200"/>
                <DataGridTextColumn Header="Šef" Binding="{Binding Sef.Ime}" Width="120"/>
                <DataGridTextColumn Header="Broj autora" Binding="{Binding SpisakAutora.Count}" Width="100"/>
                <DataGridTextColumn Header="Broj knjiga" Binding="{Binding SpisakKnjiga.Count}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Zatvori" Width="80" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,0"
                Click="BtnZatvori_Click"/>
    </Grid>
</Window>
