<Window x:Class="WpfClient.IzdavacDetaljiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient"
        xmlns:res="clr-namespace:WpfClient.Resources"
        mc:Ignorable="d"
        Title="{x:Static res:Strings.IzdavacDetaljiWindow_Title}"
        Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

       
        <StackPanel>
            <TextBlock Text="{Binding Naziv}"
                       FontSize="18"
                       FontWeight="Bold"/>

            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="{x:Static res:Strings.Izdavac_Sef_Label}"
                           FontWeight="Bold"/>
                <TextBlock x:Name="txtSef"/>
            </StackPanel>
        </StackPanel>

       
        <GroupBox Header="{x:Static res:Strings.Izdavac_Autori}"
                  Grid.Row="1"
                  Margin="0,10,0,10">
            <DataGrid x:Name="dgAutori"
                      AutoGenerateColumns="False"
                      SelectionMode="Single"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{x:Static res:Strings.Autor_Ime}"
                                        Binding="{Binding Ime}" Width="*"/>
                    <DataGridTextColumn Header="{x:Static res:Strings.Autor_Prezime}"
                                        Binding="{Binding Prezime}" Width="*"/>
                    <DataGridTextColumn Header="{x:Static res:Strings.Autor_GodIskustva}"
                                        Binding="{Binding GodineIskustva}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="2"
                    HorizontalAlignment="Right">
            <Button Content="{x:Static res:Strings.Btn_AddChief}"
                    Margin="0,0,10,0"
                    Click="BtnDodajSefa_Click"/>
            <Button Content="{x:Static res:Strings.Btn_RemoveChief}"
                    Margin="0,0,10,0"
                    Click="BtnUkloniSefa_Click"/>
            <Button Content="{x:Static res:Strings.About_Close}"
                    Click="BtnZatvori_Click"/>
        </StackPanel>
    </Grid>
</Window>